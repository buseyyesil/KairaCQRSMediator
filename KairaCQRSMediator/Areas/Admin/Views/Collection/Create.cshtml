@model KairaCQRSMediator.Features.Mediator.Commands.CollectionCommands.CreateCollectionCommand
@{
    ViewData["Title"] = "Yeni Koleksiyon";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">Yeni Koleksiyon Ekle</h4>
        <form asp-action="Create" method="post">
            <div class="form-group">
                <label asp-for="Title">Başlık</label>
                <input asp-for="Title" class="form-control" required />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Description">Açıklama</label>
                <textarea asp-for="Description" class="form-control" rows="5" required></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="ImageUrl">Resim URL</label>
                <input asp-for="ImageUrl" class="form-control" placeholder="~/kaira-1.0.0/images/foto8.jpg" required />
                <span asp-validation-for="ImageUrl" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-primary">Kaydet</button>
            <a asp-action="Index" class="btn btn-light">İptal</a>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}